repos:
  - repo: local
    hooks:
      - id: envdrift-encryption
        name: Check env encryption status
        entry: envdrift encrypt --check
        language: system
        files: ^\.env\.
        pass_filenames: true
